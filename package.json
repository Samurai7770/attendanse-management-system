{
  "name": "qr-attendance-system",
  "version": "1.0.0",
  "description": "QR Attendance System with Express backend and React frontend",
  "private": true,
  "scripts": {
    "dev": "npm --prefix ./backend start & npm --prefix ./frontend run dev",
    "start": "npm --prefix ./backend start",
    "build": "npm --prefix ./backend build && npm --prefix ./frontend run build",
    "install-all": "npm install && npm --prefix ./backend install && npm --prefix ./frontend install",
    "clean": "rm -rf backend/node_modules frontend/node_modules dist"
  },
  "keywords": [
    "qr-code",
    "attendance",
    "system"
  ],
  "author": "",
  "license": "MIT"
}
